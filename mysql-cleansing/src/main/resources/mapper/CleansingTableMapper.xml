<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyn.cleansing.mapper.CleansingTableMapper">

    <update id="cleansingTable">
        <foreach collection="sqlList" item="one" >
            ${one}
        </foreach>
    </update>

</mapper>